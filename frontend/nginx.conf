
# server {
#     listen 80;
    
#     location / {
#         root /usr/share/nginx/html;
#         index index.html index.htm;
#         try_files $uri $uri/ /index.html;
        
#         # Add custom header to identify responding node
#         add_header X-Node-ID "${NODE_ID}" always;
#     }
    
#     # Handle API requests (in production, these would be proxied to your actual API)
#     location /api/ {
#         root /usr/share/nginx/html;
#         add_header X-Node-ID "${NODE_ID}" always;
#         add_header Content-Type application/json;
#         try_files $uri $uri/ /index.html;
#     }
    
#     error_page 500 502 503 504 /50x.html;
#     location = /50x.html {
#         root /usr/share/nginx/html;
#     }
# }
C:\Program Files\Docker\Docker\resources\com.docker.diagnose

events {}

http {
  upstream frontend_cluster {
    server react-app-1:80;
    server react-app-2:80;
    server react-app-3:80;
  }

  server {
    listen 80;

    location / {
      proxy_pass http://frontend_cluster;
    }
  }
}
